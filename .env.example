# Single .env template for this repo
# Copy this file to `.env` at the repo root.
#
# The Docker Compose files are configured to read `../../.env`, so a single root
# `.env` controls both CPU and CUDA containers.

# Test Configuration
TEST_SCENARIO=baseline
TEST_DURATION=300

# Named Pipe Configuration
PIPE_NAME=AdaptiveMusicPipe

# Model Paths
MODEL_PATH=/app/models
MODEL_BASE=models/default/model_base.onnx
MODEL_TOKEN=models/default/model_token.onnx
MODEL_CONFIG=models/default/config.json

# GPU Configuration
CUDA_VISIBLE_DEVICES=0
NVIDIA_VISIBLE_DEVICES=all

# Hugging Face (optional)
# Only needed if you want containers/scripts to download models from Hugging Face.
# Get your token from: https://huggingface.co/settings/tokens
HF_TOKEN=your_huggingface_token_here

# Output Paths
METRICS_OUTPUT_PATH=/metrics
MIDI_OUTPUT_PATH=/unity/MidiTestOutput
